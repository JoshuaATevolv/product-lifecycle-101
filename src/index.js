import React from 'react';
import ReactDOM from 'react-dom/client';
import Week1Module from './Week1Module';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Week1Module />
  </React.StrictMode>
);
